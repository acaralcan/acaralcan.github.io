import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.acaralc.neonstriker',
  appName: 'Neon Striker',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
